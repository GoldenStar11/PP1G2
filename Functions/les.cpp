#include <bits/stdc++.h>

using namespace std;

// тип функции(что возвращает, если не возвращает void), название функции(параметры) 
void sqrt(int a){
    return(sqrt(a));
} 

int main(){
    int a,b;
    cin >> a >> b;
    cout << return 
}
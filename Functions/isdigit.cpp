#include <bits/stdc++.h>

using namespace std;

bool isDigit(char c){
    if(c >= '0' && c <= '9'){
        return true;
    }
    else return false;
}
f(x) = x + 2
f(2) = 2 + 2
int main(){
    char a, b;
    cin >> a;
    isDigit(a)
    if(isDigit(a, b) == true) cout << "Yes";
    else cout << "No";
}